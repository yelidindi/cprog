#include <stdio.h>

int main() {
    int main() {
    int n,i;
    scanf("%d",&n);
    for(i=0;i<=n;i++)
    {
        
        if(series fibonacci)
        printf("Yes");
        else
        printf("No");
    }
   return 0;
}
